<div class="screen">
  <div class="leftside">

    <form action="/" method="POST">
      <div>
        <h2>Comentário</h2>
        <textarea name="comentarios" rows="10" cols="50"></textarea>
      </div>
      <button id="btn-cadastrar">Cadastrar</button>
      <div class="rightside">
      </div>
    </form>
  </div>
  <div class="rightside">
    <h2>Comentários</h2>
    {{!-- possivel erro que não renderiza na tela --}}
    {{#each comentarios as | comentario | }} <br>
    <input type="submit" value="ouvir">
    <p>{{comentario}}</p> <br>
    <hr>
    {{/each}}
  </div>
</div>